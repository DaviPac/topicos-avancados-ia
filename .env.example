# Modelo do segredos.env. Copie para "segredos.env" e preencha os valores reais.
# O segredos.env NÃO é versionado (está no .gitignore).

# Chave da NVIDIA NIM — usada pelo harness de backup (opencode).
NVIDIA_NIM_API_KEY=

# O token do Overleaf NÃO fica aqui: ele é guardado no Gerenciador de
# Credenciais do Windows (host git.overleaf.com, usuário git).
